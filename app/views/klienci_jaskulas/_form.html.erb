<%= form_with(model: klienci_jaskula, local: true) do |form| %>
  <% if klienci_jaskula.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(klienci_jaskula.errors.count, "error") %> prohibited this klienci_jaskula from being saved:</h2>

      <ul>
      <% klienci_jaskula.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :imie %>
    <%= form.text_field :imie, id: :klienci_jaskula_imie %>
  </div>

  <div class="field">
    <%= form.label :nazwisko %>
    <%= form.text_field :nazwisko, id: :klienci_jaskula_nazwisko %>
  </div>

  <div class="field">
    <%= form.label :wiek %>
    <%= form.number_field :wiek, id: :klienci_jaskula_wiek %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
